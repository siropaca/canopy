# デザインシステム

IntelliJ IDEA の新 UI (ダークテーマ) に寄せる。  
値の唯一の参照元は [mock/tree.tmpl.html](mock/tree.tmpl.html) の `:root` にある CSS 変数。  
`tree.html` は生成物なので参照しない。  
実装では同じ名前の CSS 変数を `src/shared/styles/tokens.css` に置き、モックからコピーする。

**下の表の値もテストの比較対象。**  
モック・`tokens.css`・この表の 3 つが一致していないと `src/shared/styles/tokens.test.ts` が落ちる。  
hex は小文字で書く。  
Prettier が `tokens.css` を小文字に正規化するので、大文字で書くと片方だけ直せなくなる。

## 色

### 面と境界

| 変数 | 値 | 用途 |
| --- | --- | --- |
| `--bg` | `#1e1f22` | 背景、入力欄の中 |
| `--panel` | `#2b2d30` | サイドバー・タブバー・ステータスバー・メニュー・ダイアログ |
| `--head` | `#3b3e43` | リポジトリ見出しの行、ツールチップ |
| `--border` | `#393b40` | 通常の境界線 |
| `--border2` | `#4a4d51` | 強い境界線、入力欄とダイアログの枠 |
| `--head-line` | `#222426` | リポジトリ見出しの下端 |
| `--overlay-line` | `#35383d` | ダイアログのタイトルバーの区切り |
| `--on` | `#43464b` | 押している状態の背景 (サイドバー、コンソールのタブ) |
| `--on-border` | `#55585e` | 押している状態の枠 |

### 文字

| 変数 | 値 | 用途 |
| --- | --- | --- |
| `--fg` | `#dfe1e5` | 本文 |
| `--dim` | `#6f737a` | 補助テキスト |
| `--faint` | `#54585e` | 件数、無効なメニュー項目、`読み取り専用` の表示 |
| `--disabled` | `#4a4d51` | 無効なボタン、空の表示 |
| `--fg-strong` | `#fff` | 濃い背景に乗せる文字 (見出し、選択中のタブ、トースト) |
| `--fg-mid` | `#b4b8be` | トーストの副情報 |
| `--nohit` | `#787c82` | 検索でヒットが無いリポジトリ見出しの文字 |

### 選択とホバー

| 変数 | 値 | 用途 |
| --- | --- | --- |
| `--hover` | `#2f3134` | ホバー |
| `--sel` | `#3c3f41` | 選択 (フォーカスなし)、トーストの面 |
| `--selfoc` | `#2e436e` | 選択 (フォーカスあり)、メニューのハイライト |
| `--head-sel` | `#4a4e54` | リポジトリ見出しの選択 (フォーカスなし) |
| `--head-selfoc` | `#3d5a94` | リポジトリ見出しの選択 (フォーカスあり)、スプリッタのドラッグ |
| `--head-nohit` | `#33363a` | 検索でヒットが無いリポジトリ見出しの背景 |

### 意味の色

| 変数 | 値 | 用途 |
| --- | --- | --- |
| `--track` | `#548af7` | behind、リモート名、リンク、改名した変更ファイル (`R`) |
| `--ahead` | `#62a85e` | ahead |
| `--cur` | `#e8b73d` | 現在ブランチのタグアイコン |
| `--dirty` | `#e8a33d` | 未コミット |
| `--gone` | `#9a5f5f` | 追跡ブランチが消えている |
| `--folder` | `#9aa0a6` | フォルダアイコン |
| `--icon` | `#9aa0a6` | アイコンの既定色 (ブランチ、タグ、サイドバー) |
| `--wt` | `#b28fd0` | ワークツリー |

色は意味に紐づける。  
青は「取り込むもの」、緑は「出すもの」、オレンジは「手元の未確定」、紫は「別のワークツリー」。  
新しい意味を足すときは既存の 4 色を使い回さず、表に追加する。

**同じ値の別トークンを 1 つにまとめない。**  
`--folder` と `--icon`、`--border2` と `--disabled` と `--badge-bg` は値が同じだが意味が違う。  
まとめると片方だけ変えたいときに全部が動く。

### バッジと出力

濃い地に色を乗せる組み合わせ。`<意味>-bg` が地、`<意味>-fg` がその上の文字。

| 変数 | 値 | 用途 |
| --- | --- | --- |
| `--track-bg` | `#26364f` | behind バッジの地 |
| `--track-fg` | `#7ba7f5` | behind バッジの数字、トーストのリンク |
| `--ahead-bg` | `#2b3d2e` | ahead バッジの地 |
| `--ahead-fg` | `#8ec98e` | ahead バッジの数字 |
| `--dirty-bg` | `#4a3c22` | 未コミットバッジの地 |
| `--badge-bg` | `#4a4d51` | バッジの既定の地 |
| `--badge-fg` | `#cfd2d6` | バッジの既定の文字 |
| `--del` | `#c96a6a` | 削除された変更ファイル (`D`) |
| `--err` | `#c9534b` | 失敗のアイコン、タブの赤いドット |
| `--err-line` | `#e06c6c` | コンソールのエラー行 |
| `--out` | `#c8ccd2` | コンソールのコマンド行 |
| `--out-dim` | `#7d8188` | コンソールの通常の出力 |
| `--graph` | `#3a4c6d` | プッシュダイアログでコミットをつなぐ線 |
| `--menu-key` | `#cbd5ea` | メニューのハイライト時の副情報 |

### ボタン

| 変数 | 値 | 用途 |
| --- | --- | --- |
| `--btn` | `#365880` | 詳細ペインのプライマリボタン |
| `--btn-hover` | `#3f6294` | 同じもののホバー |
| `--btn-border` | `#4b6eaf` | プライマリボタンとトグルの枠 |
| `--btn-face` | `#3a3d42` | ダイアログの通常ボタンのホバー |
| `--accent` | `#4b83f0` | ダイアログのプライマリボタン、入力欄のフォーカス、トーストのアイコン |
| `--accent-hover` | `#5a8ef4` | 同じもののホバー |
| `--danger` | `#a4453f` | 強制プッシュなど破壊的操作のボタン |
| `--danger-hover` | `#b8504a` | 同じもののホバー |
| `--danger-face` | `#5a3535` | 削除系メニューのホバーの地 |
| `--danger-fg` | `#f0d4d4` | 同じときの文字 |

**影とオーバーレイの黒はトークンにしない。**  
`rgba(0,0,0,.4)` などは色ではなく効果で、意味に名前を付けられない。  
モックにもそのまま残してある。

検索欄のアイコンは data URI の中に色を書いている。  
`url()` の中では CSS 変数が使えないので、ここだけモックに生の値が残る。  
実装ではインラインの SVG にして `currentColor` で塗る。

## 寸法

| 変数 / 値 | 用途 |
| --- | --- |
| `--row: 21px` | ツリーとリストの行高 |
| `13px` | 本文のフォントサイズ |
| `12px` | 補助テキスト、ステータスバー、メニューの副情報 |
| `13px` | ツリー内のアイコン、インデントの 1 段 |
| `11px` | インジケーターの矢印 |
| `10px` | バッジ内のアイコン (見出しのサマリ) |
| `36px` | サイドバーの幅 |
| `30px` | サイドバーのボタン |
| `360px` | ツリーペインの初期幅 (240〜760px でリサイズ可) |
| `230px` | コンソールパネルの高さ |
| `26px` | ツールバー・タブバーの高さ |
| `20px` | ステータスバーの高さ |

角丸は使わない。  
例外はダイアログ (`8px`) とダイアログ内のボタン・入力欄 (`4〜5px`)。  
余白は詰める。行の左右パディングは 5〜7px。

## アイコン

- すべてインラインの SVG。`viewBox="0 0 12 12"` か `0 0 16 16`
- `fill: none` / `stroke: currentColor` / `stroke-width: 1.1〜1.35`
- 色は class で当てる。SVG に色を直接書かない
- 意味が違うものに同じ形を使わない。ブランチ、タグ、フォルダ、ワークツリーはすべて別の形

| アイコン | 意味 |
| --- | --- |
| タグ (斜め・オレンジ) | 現在のブランチ |
| ブランチ (ノード 2 つと分岐) | 通常のブランチ |
| タグ (横向き・グレー) | git のタグ |
| フォルダ | スラッシュで畳んだディレクトリ |
| ワークツリー (2 枚の四角・紫) | 別のワークツリーにチェックアウトされている |
| 斜め下の矢印 | behind (取り込む) |
| 斜め上の矢印 | ahead (出す) |
| 斜め下の破線矢印 | フェッチ (まだ取り込んでいない) |

## インジケーターの色と並び

未コミット (オレンジ) → behind (青) → ahead (緑) の順に並べる。  
行のどこに何が出るかは [specs/ui.md](specs/ui.md)。

数字は等幅にして桁がずれないようにする (`font-variant-numeric: tabular-nums`)。

## 状態の表し方

- **現在のブランチ** — オレンジのタグアイコンのみ。太字にしない、背景色を付けない
- **選択行** — 背景色。フォーカスがあれば青、なければグレー
- **無効** — 文字色を `--disabled` に落として、ホバー効果を消す

何をどの状態で出すかは [specs/ui.md](specs/ui.md)。ここは見え方だけ。

## スクロール

**ネイティブのスクロールバーは出さない。** 自前のスクロールバーを描く ([adr/0012-scrollbar-and-virtualization.md](adr/0012-scrollbar-and-virtualization.md))。  
細く、操作中だけ濃くなる。IDEA に寄せる。  
ツリーとコンソールで同じものを使う。  
仮想スクロールを使うので、スクロール量から全体の件数を推測させない前提で設計する。

## 文言

文言のルールは [specs/ui.md](specs/ui.md) にある。
