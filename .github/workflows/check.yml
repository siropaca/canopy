name: check

on:
  push:
    branches: [main]
  pull_request:

# 同じブランチへ続けて push したら古い実行を止める
concurrency:
  group: check-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    # macOS のみを対象にしている (docs/adr/0014-macos-only.md)
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7

      # Node・pnpm・Rust のバージョンは mise.toml から読む
      - uses: jdx/mise-action@v4

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      # mise が入れた Rust には rustfmt / clippy が付いてこない。
      # `cargo fmt --check` が「コンポーネントが無い」で落ちるので明示的に足す。
      # 検査そのものは pnpm check の 1 本のまま (docs/development.md の CI)
      - name: rustup components
        run: rustup component add rustfmt clippy

      - name: pnpm install
        run: pnpm install --frozen-lockfile

      # ローカルとまったく同じコマンドを回す (docs/development.md)
      - name: pnpm check
        run: pnpm check
